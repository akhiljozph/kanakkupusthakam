import type { ExpensesProps } from "./Expenses.types";

import "./Expenses.scss";

const Expenses = (props: ExpensesProps) => {
    return (
        <>
            expenses works!
        </>
    )
}
export default Expenses;